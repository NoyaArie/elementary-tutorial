version: 2
models:
  - name: groups
    columns:
      - name: group_a
        tests:
          - unique:
              meta:
                description: The group_a column in the groups table should contain
                  only unique values
