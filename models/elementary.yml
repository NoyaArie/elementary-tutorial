version: 2
models:
  - name: groups
    columns:
      - name: group_b
        tests:
          - dbt_utils.at_least_one:
              group_by_columns: # Optional, Columns to group by before executing the test
                - group_id
              config:
                severity: error
