version: 2
models:
  - name: config_levels_test_and_model
    columns:
      - name: null_count_str
        tests:
          - dbt_utils.not_empty_string:
              trim_whitespace: true # Optional
              config:
                severity: error
