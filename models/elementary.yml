version: 2
models:
  - name: string_column_anomalies
    tests:
      - elementary.freshness_anomalies:
          time_bucket:
            period: day
            count: 3
          days_back: 14
          backfill_days: 2
          sensitivity: 3.0
          timestamp_column: occurred_at
          config:
            severity: warn
          meta:
            owner:
              - blabla
            description: Checking for freshness anomalies by looking at a 3 days period
              and comparing it to last 14 days
